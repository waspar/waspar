import convert_string_decimal from './convert/string_decimal';

/**
 * wsp tools` settings common configuration interface.
 *
 * @typedef {Object}
 */
export default {
	convert: {
		string: {
			decimal: convert_string_decimal
		},
	}
};